package models;

/**
 * The current categories that an Expense can fit under. Any Category that is not strictly specified should go under "Other"
 */
public enum Category {
    FOOD, ENTERTAINMENT, RENT, VEHICLE, PHONE, OTHER
}
